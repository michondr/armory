# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.turbo/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# runtime data (mounted volumes)
/data/
images/

# editor / OS
.DS_Store
.vscode/
.idea/
.claude/

# python
__pycache__/
*.pyc

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# expo / mobile (later phases)
apps/mobile/.expo/
# Native projects are generated by `expo prebuild` in CI (managed workflow),
# so the generated android/ and ios/ trees are not committed.
apps/mobile/android/
apps/mobile/ios/

# root-owned leftovers from old docker runs; delete with sudo
*.rootowned
.pnpm-store.rootowned
